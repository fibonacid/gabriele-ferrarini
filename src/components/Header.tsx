import type { Component } from "solid-js";

const Header: Component = () => {
  return (
    <header class="h-12 flex items-center p-3">
      <h1 class="text-3xl">Gabriele Ferrarini</h1>
    </header>
  );
};

export default Header;
